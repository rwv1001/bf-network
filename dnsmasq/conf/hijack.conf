# DNS Hijacking DNSmasq Configuration — REFERENCE TEMPLATE
# This file is NOT used directly. The actual config is generated at container
# startup by dnsmasq/entrypoint.sh from PORTAL_IP and HIJACK_DNS_IP env vars.
# See dnsmasq/docker-compose.yml and dnsmasq/entrypoint.sh.
#
# listen-address=${HIJACK_DNS_IP}
# bind-interfaces
# no-resolv
# address=/#/${PORTAL_IP}
# log-queries
# log-facility=/var/log/dnsmasq-hijack.log
