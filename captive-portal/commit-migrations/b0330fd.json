{
  "description": "Remove test env vars FIRMWARE_TEST_VAR_1 and FIRMWARE_TEST_VAR_2 — no longer needed after docker-agent integration testing.",
  "env": {
    "add": [],
    "remove": [
      {
        "key": "FIRMWARE_TEST_VAR_1",
        "description": "Test env var — was added to verify firmware update env handling",
        "required": false,
        "sensitive": false
      },
      {
        "key": "FIRMWARE_TEST_VAR_2",
        "description": "Test env var — was added to verify firmware update env handling",
        "required": true,
        "sensitive": false
      }
    ]
  },
  "db": {
    "up":   "commit-migrations/b0330fd-up.sh",
    "down": "commit-migrations/b0330fd-down.sh",
    "verify_up": [
      { "type": "table_absent",  "name": "firmware_test_table" },
      { "type": "column_absent", "table": "users", "column": "firmware_test_col" }
    ],
    "verify_down": [
      { "type": "table",  "name": "firmware_test_table" },
      { "type": "column", "table": "users", "column": "firmware_test_col" }
    ]
  }
}
